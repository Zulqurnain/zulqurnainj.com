name: Add tools.zulqurnainj.com CNAME

on:
  workflow_dispatch:

permissions:
  issues: write

jobs:
  cname:
    runs-on: ubuntu-latest
    env:
      H: ${{ secrets.CPANEL_HOST }}
      U: ${{ secrets.CPANEL_USER }}
      P: ${{ secrets.CPANEL_PASS }}
      D: zulqurnainj.com
      GT: ${{ secrets.GITHUB_TOKEN }}
      GR: ${{ github.repository }}

    steps:
      - uses: actions/checkout@v4
      - name: install dig
        run: sudo apt-get install -y dnsutils -q
      - name: add tools CNAME record pointing to Vercel
        run: python3 scripts/add_tools_cname.py
